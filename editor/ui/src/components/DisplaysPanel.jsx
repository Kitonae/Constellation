import React from 'react'

export default function DisplaysPanel() {
  return (
    <div style={{ padding: 16, color: 'var(--text)' }}>
      <h3 style={{ margin: '0 0 8px 0', fontWeight: 600 }}>Displays</h3>
      <p style={{ margin: 0, opacity: 0.8 }}>
        This panel is not yet implemented in the UI. Use the 2D or 3D View for now,
        or reopen display windows from the menu to preview output.
      </p>
    </div>
  )
}
